/* Questions
	Define:
		a) casting
		b) parsing
		c) conversion

	True or false?
		a) casting double to int is unproblematic 
		b) casting int to double is unproblematic
		c) the number 10 is saved as [10], whereas the string "10" is saved as [49, 48]
		d) "dynamic" is the C# equivalent of VBA "variant"
		e) "var" is the C# equivalent of VBA "variant"
		f) when we do not declare the type, then the program has to do it on runtime
		g) whenever possible, we try to avoid variant types
		h) custom types cannot be cast
*/