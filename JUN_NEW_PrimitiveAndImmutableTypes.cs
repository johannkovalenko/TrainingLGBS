/* Questions
	True or false?
		a) "int" is a primitive type
		b) primitive types are saved on the heap (sterta)
		c) C# has the type "byte" which has 8 bits
		d) primitive types are reference types
		e) the type "bool" has one bit
		f) to declare a primitive type, we do not need to create an instance ("new") of it
		g) string is an immutable type. this means, that it is an value type, although it is a array of chars (arrays are reference types).
		i) primitive types (=value types) are passed by value.
		j) "string" is an array of "chars"
		
*/