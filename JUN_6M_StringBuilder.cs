/* Questions

	Which of below statements holds true for a "StringBuilder" and which for "Concatenation".
	a) Requires an instance
	b) Appends the part to be added to the existing string, not touching the existing part
	c) Takes the existing part (A), adds the part to be added (B), and overwrites the whole existing part (A)
	d) To be preferred with long strings and frequent string operations
*/