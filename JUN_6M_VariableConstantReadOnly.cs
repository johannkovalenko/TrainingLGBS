/* Questions
	True or false?
		a) we do not use "public static" variables
		b) we can make instances of a constant 
		c) to declare a constant in C#, we write "public static const myConst"
		d) a constant is put to the stack on launch time and is read-only
		e) a variable cannot be "const" and/or "static" and/or readonly at the same time
		f) the value for "readonly" can only be assigned once in the constructor, but for every instance it can have another value.
		g) we prefer a constant over a variable, when we have to do with fixed input values.
*/